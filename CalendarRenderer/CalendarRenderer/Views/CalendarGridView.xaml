<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:CalendarRenderer.Views"
             xmlns:app="clr-namespace:CalendarRenderer"
             x:Class="CalendarRenderer.Views.CalendarGridView">

    <ContentView.Content>
        <ListView HasUnevenRows="true">

            <ListView.Triggers>
                <DataTrigger TargetType="ListView" Binding="{Binding DisplayMode}" Value="0">
                    <Setter Property="ItemsSource" Value="{Binding CurrentMonth.Weeks}"/>
                    <Setter Property="ItemTemplate" Value="{StaticResource Key=DataTemplateGridMonth}"/>
                </DataTrigger>
                <DataTrigger TargetType="ListView" Binding="{Binding DisplayMode}" Value="1">
                    <Setter Property="ItemsSource" Value="{Binding Years}"/>
                    <Setter Property="ItemTemplate" Value="{StaticResource Key=DataTemplateGridYear}"/>
                </DataTrigger>
            </ListView.Triggers>

        </ListView>
    </ContentView.Content>
</ContentView>